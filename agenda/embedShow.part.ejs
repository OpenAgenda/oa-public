<% if ( !total ) { %>
<% if ( ( passed === true ) && !hasSearchQuery ) { %>
    <p><%- __( 'There are no upcoming events published in this agenda.' ) %></p>
<% } else { %>
    <p><%= __( 'There are no results for this search' ) %></p>
<% } %>
<% } else { %>

<% renders.eventItems.forEach(function( eventRender ) { %>
    <%- eventRender %>
<% } ); %>

<% } %>