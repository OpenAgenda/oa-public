# Click tracker

Callback when a click was made out of a targeted element

Initialize by giving the selector of the element and the callback to call

    clickTracker( 'search', '.search', callback );

Switch on with the switchOn method with the name of the tracker:

    clickTracker.switchOn( 'search' );

Same with switchOff

    
