"use strict";

let config;
let knex;

module.exports = Object.assign( update, { init } );

function init( { config: c, knex: k } ) {

  config = c;
  knex = k;

}

function update( entityType, entityUid ) {
};
