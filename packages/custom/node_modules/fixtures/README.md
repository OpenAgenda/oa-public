    const fixtures = require( 'fixtures' );

    let mysql = { database: 'dbname', host: ... }; // mysql connection params

    fixtures.init( { mysql } );

    fixtures( [{
      table: 'user',
      src: __dirname + '/user.data.sql'
    }, {
      table: 'review',
      src: __dirname + '/review.data.sql'
    }], cb );