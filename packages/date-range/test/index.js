"use strict";

var should = require( 'should' );

describe( 'date-range', () => {

  it( 'if one date, displays as "10 december, 15:00, 17:00, 19:00"' );

  it( 'if two dates, displays as "4 and 10 december"' );

  /* add tests as per readme description */

} );
