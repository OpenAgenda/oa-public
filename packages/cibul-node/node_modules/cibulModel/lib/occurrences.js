module.exports = function(config) {

  model = require('./model')(config);

  return {
    getByLocationId: getByLocationId
  }

};

var model,

lib = require('./lib'),

getByLocationId = function(locationId, callback) {

  var con = model.getConnection();

  con.query('select * from occurrence where location_id = ?', locationId, callback);

};