<mjml>
  <mj-head>
    <mj-title><%= __( 'title', { agenda, event } ) %></mj-title>
    <mj-preview><%= __( 'title', { agenda, event } ) %></mj-preview>

    <mj-attributes>
      <mj-all
        font-family="'Helvetica Neue',Helvetica,Arial,sans-serif"
        line-height="22px"
      />
    </mj-attributes>

    <mj-style inline="inline">
      h1 {
        font-size: 22px;
        font-style: normal;
        font-weight: normal;
        line-height: 1.4em;
        letter-spacing: normal;
      }

      .title {
        margin-top: 0;
      }

      .logo {
        max-width: 300px;
      }

      .center {
        text-align: center;
      }

      .grey-link {
        color: #555555;
      }

      a .grey-link:hover {
        color: #41ACDD;
      }
    </mj-style>
  </mj-head>

  <mj-body
    background-color="#f4f4f4"
  >
    <!-- Header -->
    <mj-section padding="15px 0">
      <mj-column>
        <mj-image
          css-class="logo"
          width="<%= logo.width || '300px' %>"
          src="<%= logo.src %>"
          href="<%= link %>"
          padding="0"
          alt="logo"
        />
      </mj-column>
    </mj-section>

    <mj-wrapper padding="0" border="1px solid #eeeeee">
      <!-- Content -->
      <mj-section
        background-color="#ffffff"
        padding="20px 40px"
      >
        <mj-column>
          <mj-text>
            <h1 class="title"><%= __( 'title', { agenda, event } ) %></h1>
          </mj-text>

          <mj-markdown><%- __( 'body', { event, sourceAgenda, state: __( state ) } ) %></mj-markdown>

          <mj-button
            background-color="#41acdd"
            color="white"
            align="center"
            href="<%= link %>"
          >
            <%= __( 'actionDescription' ) %>
          </mj-button>
        </mj-column>
      </mj-section>

      <!-- Footer -->
      <mj-section padding="15px 0">
        <mj-column>
          <mj-image
            width="40px"
            src="https://s3.eu-central-1.amazonaws.com/oastatic/avatar40.png"
            href="<%= root %>"
            padding="0"
            alt="footer icon"
          />

          <mj-text align="center">
            <% if ( typeof memberUnsubscribeLink !== 'undefined' ) { %>
            <div>
              <a href="<%= memberUnsubscribeLink %>" class="grey-link">
                <%= __( 'memberUnsubscribe' ) %>
              </a>
            </div>
            <% } %>

            <div>
              <a href="<%= unsubscribeLink %>" class="grey-link">
                <%= __( 'unsubscribe' ) %>
              </a>
            </div>

            <div>
              <a href="<%= emailSettingsLink %>" class="grey-link">
                <%= __( 'changeEmailSettings' ) %>
              </a>
            </div>
          </mj-text>
        </mj-column>
      </mj-section>
    </mj-wrapper>
  </mj-body>
</mjml>
