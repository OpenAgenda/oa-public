describe( 'user service', function( ) {

  it( 'authenticate - should show an error for non activated account' );
  it( 'authenticate - should show an error for erroneous password' );
  it( 'authenticate - should show an error for user not found' );
  it( 'authenticate - should validate attempt with correct password' );

} );