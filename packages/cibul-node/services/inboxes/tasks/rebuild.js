"use strict";

const usersSvc = require( '@openagenda/users' );
const agendasSvc = require( '@openagenda/agendas' );

// loop on users that don't have inbox
// loop on agendas:
//  - create inbox if not exists
//  - loop on stakeholders (adminmods):
//    - create inboxUser if not exists

module.exports = () => {

  //

};
