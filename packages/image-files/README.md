# Overview

Handle image processing from when an image url or temporary path to the moment it is stored on s3. Does formatting through the 'images' service and file uploading through the 'files' service. Does not handle client to server image uploads.

# Testing

Once test configuration is loaded, run mocha

# Configuration

Look at config.sample.js for inspiration. Save config.js on the root of the project for inspiration.