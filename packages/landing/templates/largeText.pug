div(class='background-' + ( background || 'white' ))
  .container
    h2.margin-bottom-lg.margin-top-lg.text-line-bg!=title
    .margin-top-lg
      if typeof text !== 'undefined'
        p!=text
      if typeof points !== 'undefined'
        ul
          each p in points
            li 
              p!=p