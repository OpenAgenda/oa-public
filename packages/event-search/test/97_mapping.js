'use strict';

const baseMapping = require('../../service/index/settings.json').mappings.properties;

const mergeSchemaIntoMapping = require('../../helpers/mergeSchemaIntoMapping');

describe('mergeSchemaIntoMapping', () => {

  /*it('radio and checkbox types', () => {
    mergeSchemaIntoMapping(baseMapping, {
      fields: [{

      }]
    }));
  });*/

});
