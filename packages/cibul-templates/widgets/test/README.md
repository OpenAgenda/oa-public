not markdown.

Tests:

if auto sync is ticked, and I move the map, href, list & other widgets update with coordinates of corners

if marker is clicked, location filter is activated

if marker is clicked again, location filter is de-activated

if category or tag or dates ( any filter value ) is selected, map encompasses all markers with active values

when something is typed in 'what', location filter is cancelled ( what is often a city name )