<%
const nonceAttr = typeof cspNonce !== 'undefined' ? ` nonce="${cspNonce}"` : '';
%><% if ( typeof bottom !== 'undefined' ) { %>
<% if ( bottom.scripts ) { %>
<% bottom.scripts.forEach( function( script ) { %>
<script type="text/javascript"<%- nonceAttr; %>><%- script %></script>
<% }); %>
<% } %>
<% } %>
  </body>
</html>
