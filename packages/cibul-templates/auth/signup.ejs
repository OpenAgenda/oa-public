<% if ( agenda ) { %>
  <% include ../agenda/head.part.ejs %>
<% } %>
<div class="container page-dialog signup">
  <div class="top-margined bottom-margined">
    <% include _headMessage.ejs %>
    <div class="row">
      <section class="col-sm-8 col-sm-offset-2 wsq padding-all-md">
        <div class="row">
          <div class="col-sm-12 margin-h-md">
            <h2><%= __( 'createAccount' ) %></h2>
          </div>
        </div>
        <div class="row">
          <div class="col-sm-12">
            <div class="info-block text-center margin-v-lg margin-h-md">
              <label><%= __( 'hasAccount' ) %></label>
              <a href="<%= signin %>" class="btn btn-primary"><%= __( 'connect' ) %></a>
            </div>
          </div>
        </div>
        <% if ( typeof message !== 'undefined' && message ) { %>
          <div class="row">
            <div class="col-sm-12">
              <p class="margin-h-md bg-danger padding-all-sm"><%= __( message ) %></p>
            </div>
          </div>
        <% } %>
        <div class="row margin-top-md">
          <div class="col-md-<%= enabledServices.length ? 6 : 12 %>">
            <form id="signup-form" class="margin-h-md" method="post">
              <div class="form-group<%= errors.fullName ? ' has-error' : '' %>">
                <label for="full_name" class="control-label"><%= __( 'nameSurname' ) %></label>
                <input name="full_name" class="form-control" value="<%= full_name %>" />
                <% if ( errors.fullName ) { %>
                  <span class="text-danger"><%= __( errors.fullName ) %></span>
                <% } %>
              </div>
              <div class="form-group<%= errors.email ? ' has-error' : '' %>">
                <label for="email" class="control-label"><%= __( 'email' ) %></label>
                <input name="email" class="form-control" type="email" value="<%= email %>" />
                <% if ( errors.email ) { %>
                  <span class="text-danger"><%= __( errors.email ) %></span>
                <% } %>
              </div>
              <div class="form-group<%= errors.password ? ' has-error' : '' %>">
                <label for="password" class="control-label"><%= __( 'password' ) %></label>
                <input name="password" class="form-control" type="password" />
                <% if ( errors.password ) { %>
                  <span class="text-danger"><%= __( errors.password ) %></span>
                <% } %>
              </div>
              <div class="form-group<%= errors.repeat ? ' has-error' : '' %>">
                <label for="repeat" class="control-label"><%= __( 'repeatPassword' ) %></label>
                <input name="repeat" class="form-control" type="password" />
                <% if ( errors.repeat ) { %>
                  <span class="text-danger"><%= __( errors.repeat ) %></span>
                <% } %>
              </div>
              <div class="form-group<%= errors.culture ? ' has-error' : '' %>">
                <label for="culture" class="control-label"><%= __( 'culture' ) %></label>
                <select
                  name="culture"
                  class="form-control"
                  value="<%= typeof culture !== 'undefined' ? culture : 'fr' %>"
                >
                  <option value="fr" <%= typeof culture === 'undefined' || !culture ? '' : 'selected' %>>
                    Français
                  </option>
                  <option value="en" <%= typeof culture !== 'undefined' && culture === 'en' ? 'selected' : '' %>>
                    English
                  </option>
                  <option value="de" <%= typeof culture !== 'undefined' && culture === 'de' ? 'selected' : '' %>>
                    Deutsch
                  </option>
                  <option value="es" <%= typeof culture !== 'undefined' && culture === 'es' ? 'selected' : '' %>>
                    Español
                  </option>
                  <option value="br" <%= typeof culture !== 'undefined' && culture === 'br' ? 'selected' : '' %>>
                    Brezhoneg
                  </option>
                </select>
                <% if ( errors.culture ) { %>
                  <span class="text-danger"><%= __( errors.culture ) %></span>
                <% } %>
              </div>
              <% if ( mtCaptchaEnabled ) { %>
                <div class="form-group<%= errors.captcha ? ' has-error' : '' %>">
                  <div id="mtcaptcha-local"></div>
                  <% if ( errors.captcha ) { %>
                    <span class="text-danger"><%= __( errors.captcha ) %></span>
                  <% } %>
                </div>
              <% } %>
              <div class="form-group">
                <button id="signup-submit" class="btn btn-primary" type="submit"><%= __( 'signup' ) %></button>
              </div>
            </form>
          </div>
          <% if ( enabledServices.length ) { %>
            <div class="col-md-6">
              <div class="margin-all-md">
                <% if ( enabledServices.includes( 'facebook' ) ) { %>
                  <div class="margin-bottom-md">
                    <form
                      id="signup-facebook"
                      action="<%= genUrl(
                        agenda ? 'agendaFacebookSignup' : 'facebookSignup',
                        [ optionals, agenda ? { slug: agenda.slug } : {} ]
                      ) %>"
                      method="POST"
                    >
                      <button
                        id="signup-facebook-button"
                        class="btn btn-facebook btn-block"
                        type="submit"
                      >
                        <i class="fa fa-facebook"></i>
                        Facebook
                      </button>
                    </form>
                    <% if ( errors.facebookUid ) { %>
                      <span class="text-danger"><%= __( errors.facebookUid ) %></span>
                    <% } %>
                  </div>
                <% } %>
                <% if ( enabledServices.includes( 'twitter' ) ) { %>
                  <div class="margin-bottom-md">
                    <form
                      id="signup-twitter"
                      action="<%= genUrl(
                        agenda ? 'agendaTwitterSignup' : 'twitterSignup',
                        [ optionals, agenda ? { slug: agenda.slug } : {} ]
                      ) %>"
                      method="POST"
                    >
                      <button
                        id="signup-twitter-button"
                        class="btn btn-twitter btn-block"
                        type="submit"
                      >
                        <i class="fa fa-twitter"></i>
                        Twitter
                      </button>
                    </form>
                    <% if ( errors.twitterUid ) { %>
                      <span class="text-danger"><%= __( errors.twitterUid ) %></span>
                    <% } %>
                  </div>
                <% } %>
                <% if ( enabledServices.includes( 'google' ) ) { %>
                  <div class="margin-bottom-md">
                    <form
                      id="signup-google"
                      action="<%= genUrl(
                        agenda ? 'agendaGoogleSignup' : 'googleSignup',
                        [ optionals, agenda ? { slug: agenda.slug } : {} ]
                      ) %>"
                      method="POST"
                    >
                      <button
                        id="signup-google-button"
                        class="btn btn-google btn-block"
                        type="submit"
                      >
                        <i class="fa fa-google"></i>
                        Google
                      </button>
                    </form>
                    <% if ( errors.googleUid ) { %>
                      <span class="text-danger"><%= __( errors.googleUid ) %></span>
                    <% } %>
                  </div>
                <% } %>
              </div>
            </div>
          <% } %>
        </div>
        <div class="row">
          <div class="col-sm-12">
            <p class="margin-all-md">
              <%- __( 'agree', {
                terms: '<a target="_blank" href="/termsofuse">' + __( 'termsOfUse' ) + '</a>',
                policy: '<a target="_blank" href="https://doc.openagenda.com/confidentialite/">' + __(
                  'policy' ) + '</a>'
              } ) %>
            </p>
          </div>
        </div>
      </section>
    </div>
  </div>
</div>
