<div class="header">
  <h2><%= __('Template') %></h2>
  <p><%= __('Define the template of the campaign') %></p>
</div>
<div class="col w33">

<form class="cform js_campaign_layout_form" method="post" action="<%= genUrl('campaignComplete', { uid: uid }) %>">

<% if (type=="manual") { %>
    <section>
      <div class="header">
        <h3><%= __('Edito') %></h3>
      </div>
      <textarea class="js_frame_reload" rows="4" name="edito"><%= values.edito ? values.edito : "" %></textarea>
<% if ( errors.edito ) { %>
      <span class="error"><%= errors.edito %></span>
<% } %>
    </section>

    <section>
      <div class="header">
        <h3><%= __('Featured Events') %></h3>
      </div>
      <a class="button js_lightbox_trigger" href="<%= genUrl('campaignFeaturedEdit', { uid: uid }) %>"><%= __('Select Events') %></a>
    </section>
<% } %>
    <section>
      <div class="header">
        <h3><%= __('Event List') %></h3>
      </div>
      
      <h4><%= __('Categories') %></h4>

      <select class="js_frame_reload" name="category">
        <option value=""><%= __('all categories') %></option>
<% categories.forEach(function(category) { %>
        <option value="<%= category.slug %>" <% if (values.filters.category==category.slug) { %>selected<% } %>><%= category.label %></option>
<% }); %>
      </select>

      <h4><%= __('Places and Geography') %></h4>

      <div class="js_folded">

        <span class="info js_title"><%= __('select a group') %></span>
        
        <ul>
          <li class="subtitle js_subtitle"><%= __('By city') %></li>
          <li class="js_detail">
            <ul class="line">
  <% cities.forEach(function(city) { %>
              <li>
                <input class="js_frame_reload" type="checkbox" name="cities[<%= city.label %>]" <% if ( values.filters.cities.indexOf( city.slug ) !== -1 ) { %>checked<% } %>/>
                <label><%= city.label %></label>
              </li>
  <% }); %>
            </ul>
          </li>
          <li class="subtitle js_subtitle"><%= __('By department') %></li>
          <li class="js_detail">
            <ul class="line">
  <% departments.forEach(function(department) { %>
              <li>
                <input class="js_frame_reload" type="checkbox" name="departments[<%= department.slug %>]" <% if ( values.filters.departments.indexOf( department.slug ) !== -1 ) { %>checked<% } %>/>
                <label><%= department.label %></label>
              </li>
  <% }); %>
            </ul>
          </li>
          <li class="subtitle js_subtitle"><%= __('By region') %></li>
          <li class="js_detail">
            <ul class="line">
  <% regions.forEach(function(region) { %>
              <li>
                <input class="js_frame_reload" type="checkbox" name="regions[<%= region.slug %>]" <% if ( values.filters.regions.indexOf( region.slug ) !== -1 ) { %>checked<% } %>/>
                <label><%= region.label %></label>
              </li>
  <% }); %>
            </ul>
          </li>
        </ul>
      </div>

      <h4><%= __('Segmentation') %></h4>

      <select class="js_frame_reload" name="segmentation">
        <option value=""><%= __('no segmentation') %></option>
        <option value="category"><%= __('by category') %></option>
        <option value="day"><%= __('by day') %></option>
        <option value="department"><%= __('by department') %></option>
        <option value="region"><%= __('by region') %></option>
        <option value="city"><%= __('by city') %></option>
      </select>

      <div class="info"><%= __('Choose the item you want to see your event list divided') %></div>

    </section>

    <section>
      <button type="submit"><%= __('Save') %>
    </section>

  </form>

</div><!--
--><div class="col w66">
  <iframe name="newsletter-frame" height="1000" style="width: 100%;" src="<%= genUrl('newsletterShow', { uid: uid }) %>"></iframe>
</div>
<script type="text/javascript">
  run({
    preview: '<%= genUrl('campaignLayoutUpdate', { uid: uid }) %>'
  });
</script>