<div class="header">
  <h2><%= __('Newsletter') %></h2>
  <p><%= __('Manage the emailing campaigns of the agenda') %></p>
</div>
<section>
  <div class="header">
    <h3><%= __('Campaigns') %></h3>
  </div>
<% if (campaigns.length) { %>
  <ul class="glist">
<% campaigns.forEach(function(campaign) { %>
    <li>
      <a class="button" href="<%= genUrl('campaignRemove', { uid: campaign.uid }) %>"><%= __('remove') %></a>
      <a class="button" href="<%= genUrl('campaignLayoutEdit', { uid: campaign.uid }) %>"><%= __('edit layout') %></a>
      <a class="hurl title" href="<%= genUrl('campaignEdit', { uid: campaign.uid }) %>"><%= campaign.title %></a>
      <ul class="detail">
        <li>
          <label><%= __('associated list') %></label>
<% if (campaign.contactList !== null) { %>
          <a class="url" href="<%= genUrl('contactListShow', { uid: campaign.contactList.uid }) %>"><%= campaign.contactList.title %></a>
<% } else { %>
          <span><%= __('no list') %></span>
<% } %>
        </li>
<% if ( campaign.scheduledAt ) { %>
        <li>
          <span><%= __('scheduled on %value%', { '%value%' : _t( campaign.scheduledAt, 'Do MMMM YYYY, HH:mm' ) }) %></span>
        </li>
        <li>
          <a class="url" href="<%= genUrl('campaignEdit', { uid: campaign.uid }) %>"><%= __('edit') %></a>
        </li>
<% } %>
      </ul>
    </li>
<% }) %>
  </ul>
<% } else { %>
  <p><%= __('You did not configure any campaigns yet') %></p>
<% } %>
  <a class="button" href="<%= genUrl('campaignNew') %>"><%= __('Create a campaign') %></a>
</section>
<section>
  <div class="header">
    <h3><%= __('Contact lists') %></h3>  
  </div>
<% if (contactLists.length) { %>
  <ul class="glist">
<% contactLists.forEach(function(contactList) { %>
    <li>
      <a class="button" href="<%= genUrl('contactListRemove', { uid: contactList.uid }) %>"><%= __('remove') %></a>
      <a class="hurl title" href="<%= genUrl('contactListShow', { uid: contactList.uid }) %>"><%= contactList.title %></a>
      <ul class="detail">
        <li>
          <span><%= __('%s% contacts', { '%s%' : contactList.total}) %></span>
        </li>
        <li>
          <a class="url" href="<%= genUrl('contactListShow', { uid: contactList.uid }) %>"><%= __('edit') %></a>
        </li>
      </ul>
    </li>
<% }); %>
  </ul>
<% } else { %>
  <p><%= __('You did not create any contact lists yet') %></p>
<% } %>
  <a class="button" href="<%= genUrl('contactListNew') %>"><%= __('Create a contact list') %></a>
</section>