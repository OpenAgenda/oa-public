---
'@openagenda/browserslist-config': major
---

Modernize the browser targets: `chrome >= 64`, `edge >= 79`, `firefox >= 67`, `opera >= 51`, `safari >= 12` and `unreleased versions` replace the previous `firefox >= 43` / `chrome >= 42` floor. Builds extending this config drop support for older browsers.
