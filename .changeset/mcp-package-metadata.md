---
'@openagenda/mcp': patch
---

Add npm package metadata for discoverability: `keywords` (mcp, model-context-protocol, openagenda, events, …), `author`, a `bugs` URL, and `repository.directory` so the npm listing links to the package subfolder in the mirror.
