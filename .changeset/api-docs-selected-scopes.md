---
'@openagenda/api-docs': patch
---

Pre-tick `locations:read` and `me:read` in the OAuth playground, matching the read surface (locations and `/me/agendas` landed after the initial pre-tick list).
