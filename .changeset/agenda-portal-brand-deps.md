---
'@openagenda/agenda-portal': minor
---

Brand update: primary color switches from `#41acdd` to `#1d77ce`. The consent banner is imported through the `@openagenda/react-shared` bare specifier (compatible with its new exports map), and the client bundles are rebuilt with `react-intl` 10 and React 19.2.
